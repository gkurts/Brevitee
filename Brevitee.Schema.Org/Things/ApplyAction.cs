using System;

namespace Brevitee.Schema.Org
{
	///<summary>The act of registering to an organization/service without the guarantee to receive it. NOTE(goto): should this be under InteractAction instead?Related actions:RegisterAction: Unlike RegisterAction, ApplyAction has no guarantees that the application will be accepted.</summary>
	public class ApplyAction: OrganizeAction
	{
	}
}
