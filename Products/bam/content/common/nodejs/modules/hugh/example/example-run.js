var tester = require("./../module-tester");
tester.run({
  countryConfig: "./example/example-country-config", // path to country config
  moduleConfig:  "./example/example-module-list" // path to module config
});
